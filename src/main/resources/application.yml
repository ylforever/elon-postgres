server:
 servlet:
  context-path: /elon-postgres
 port: 9527

logging:
 level:
  com:
   postgres:
    mapper: debug

spring:
 datasource:
  yzy007:
   driver-class-name: org.postgresql.Driver
   jdbcUrl: jdbc:postgresql://127.0.0.1:5432/yzy007
   username: postgres
   password: root